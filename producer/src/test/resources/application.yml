server:
  port: 0

spring:
  application:
    name: streamflow-producer-test
  kafka:
    bootstrap-servers: ${TEST_KAFKA_BOOTSTRAP:localhost:9092}

management:
  endpoints:
    web:
      exposure:
        include: health, prometheus, metrics